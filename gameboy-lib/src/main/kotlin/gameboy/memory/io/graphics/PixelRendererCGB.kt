package gameboy.memory.io.graphics

import gameboy.memory.Mmu

class PixelRendererCGB(lcd: Lcd, mmu: Mmu) : PixelRenderer(lcd, mmu) {

    override fun renderPixel(bgPixel: Pixel, oamPixel: Pixel?) {

    }
}